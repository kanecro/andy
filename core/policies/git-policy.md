# Git Policy

- Do not commit unless the user asks.
- Do not push unless the user asks or the workflow explicitly requires it.
- Never force push without explicit confirmation.
- Prefer small logical commits.
- Before commit, inspect status and diff summary.
- Exclude temporary interpretation/review artifacts unless the user explicitly wants them versioned.
