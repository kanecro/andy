# GENERATED by scripts/generate-commands.py from adapters/commands/catalog.json. Do not edit directly.
description = "andy spec — turn an approved proposal into design, tasks, delta-spec, traceability"
prompt = """
You are running the andy `spec` workflow.

Input: {{args}}

Steps:

1. Resolve the andy harness root. Use the first directory that contains both `AGENTS.md` and `core/workflows/`, in this order: `$ANDY_HARNESS_ROOT` (if set), `${CODEX_HOME:-$HOME/.codex}/active-harness`, `${CODEX_HOME:-$HOME/.codex}/harnesses/andy`, `./.andy/harness`. If none exists, tell the user to run the andy installer.
2. Read `<harness-root>/core/workflows/command-router.md` and `<harness-root>/core/workflows/spec.md`, then follow them.
3. Treat the current working directory as the target development repository. Read `./openspec/changes/<change-name>/proposal.md` and write `design.md` / `tasks.md` / `specs/<feature>/delta-spec.md` / `traceability.md` under `./openspec/changes/<change-name>/`.
4. If the input is `issue #123`, locate an existing change whose proposal references the issue; if none exists, run brainstorm first and stop for approval.
5. Stop for user approval before moving to `implement`.

If the input is empty, ask the user which change-name or proposal to spec.
"""
