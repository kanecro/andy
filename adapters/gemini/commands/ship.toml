# GENERATED by scripts/generate-commands.py from adapters/commands/catalog.json. Do not edit directly.
description = "andy ship — run the full lifecycle with approval gates"
prompt = """
You are running the andy `ship` workflow (full pipeline with approval gates).

Input: {{args}}

Steps:

1. Resolve the andy harness root. Use the first directory that contains both `AGENTS.md` and `core/workflows/`, in this order: `$ANDY_HARNESS_ROOT` (if set), `${CODEX_HOME:-$HOME/.codex}/active-harness`, `${CODEX_HOME:-$HOME/.codex}/harnesses/andy`, `./.andy/harness`. If none exists, tell the user to run the andy installer.
2. Read `<harness-root>/core/workflows/command-router.md` and `<harness-root>/core/workflows/ship.md`, then follow them.
3. Treat the current working directory as the target development repository for all artifacts and code changes.
4. Use the input as the brainstorm input (topic or GitHub issue).
5. Run `brainstorm -> approval -> spec -> approval -> implement -> review -> test -> compound`. Do not skip the approval gates after brainstorm and spec.

If the input is empty, ask the user for the topic or issue to ship.
"""
