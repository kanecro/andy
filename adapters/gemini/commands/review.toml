# GENERATED by scripts/generate-commands.py from adapters/commands/catalog.json. Do not edit directly.
description = "andy review — review changes for spec compliance, quality, and risk"
prompt = """
You are running the andy `review` workflow.

Input: {{args}}

Steps:

1. Resolve the andy harness root. Use the first directory that contains both `AGENTS.md` and `core/workflows/`, in this order: `$ANDY_HARNESS_ROOT` (if set), `${CODEX_HOME:-$HOME/.codex}/active-harness`, `${CODEX_HOME:-$HOME/.codex}/harnesses/andy`, `./.andy/harness`. If none exists, tell the user to run the andy installer.
2. Read `<harness-root>/core/workflows/command-router.md` and `<harness-root>/core/workflows/review.md`, then follow them.
3. Treat the current working directory as the target development repository. Review the current diff or the change named in the input.
4. This is a read-only critique. Do not modify code; report findings with severity and evidence.
"""
