# GENERATED by scripts/generate-commands.py from adapters/commands/catalog.json. Do not edit directly.
description = "andy brainstorm — turn an idea or GitHub issue into a proposal"
prompt = """
You are running the andy `brainstorm` workflow.

Input: {{args}}

Steps:

1. Resolve the andy harness root. Use the first directory that contains both `AGENTS.md` and `core/workflows/`, in this order: `$ANDY_HARNESS_ROOT` (if set), `${CODEX_HOME:-$HOME/.codex}/active-harness`, `${CODEX_HOME:-$HOME/.codex}/harnesses/andy`, `./.andy/harness`. If none exists, tell the user to run the andy installer.
2. Read `<harness-root>/core/workflows/command-router.md` and `<harness-root>/core/workflows/brainstorm.md`, then follow them.
3. Treat the current working directory as the target development repository. Write artifacts there (`./openspec/changes/<change-name>/proposal.md`), never into the harness directory.
4. If the input looks like `issue #123` or `owner/repo#123`, resolve it as a GitHub issue per the router (prefer `gh issue view`). If it cannot be fetched, ask the user for the issue text. Otherwise treat the input as the free-form topic.
5. Stop for user approval before moving to `spec`.

If the input is empty, ask the user for the topic or issue to brainstorm.
"""
